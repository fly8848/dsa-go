# 仓库指南

## 项目结构

数据结构与算法学习仓库，按主题推进：每个主题一个目录（如 `01-array`、`02-linked-list`），内含 Go 实现、测试与短笔记；主题顺序与进度跟踪见顶层 `README.md`。

## 构建、测试与开发命令

- `go build ./...` 编译所有包。
- `go test ./...` 运行所有包测试。
- `go vet ./...` 基线静态检查。
- 测试或提交前先 `gofmt -w <changed.go>`。
- 零第三方依赖；`container/list`、`container/heap` 等标准库结构留作实现完后的对照参考，不直接用来交作业。

## 编码约定

- 使用 `gofmt` 与惯用 Go 命名；每个主题目录名即包名。
- 数据结构先自己实现，实现完可对照标准库源码学习更优做法。
- 同一主题的接口保持稳定（如链表的 `Insert/Delete/Search/Traverse`、栈的 `Push/Pop/Peek/IsEmpty`），测试依赖这些签名。

## 测试指南

- 每个主题必须有 `*_test.go`，`go test ./...` 与 `go vet ./...` 全绿才能进入下一主题。
- 关键测试类型：边界（空结构、单元素、满容量）、一致性（写入后读回）、顺序（插入后遍历顺序正确）、压力（大量随机操作验证不变量不破）。

## 协作方式

- 用户以学习数据结构与算法为主要目的：默认**引导式推进**——先讲清思路、关键不变量与验证方式，让用户手搓代码；仅在被明确要求时才直接实现。
- 引导式标准流程：先讲"为什么"（设计思路与纠错点）→ 给关键 API 与验证方式 → 搭基础架子 → 用户手搓实现 → review → 测试全绿后再推进。
- 搭架子规范：结构体与方法签名预先立好；方法体留 `panic("TODO")`（保证 `go build`/`go vet` 全绿）；import 不预置，由用户实现时自己补充；测试也要引导用户写，不代劳。
- 引导粒度：注释只给**粗粒度提示**——设计思路、关键不变量、纠错点、自查点；不给分步步骤、具体语句或返回值细节，避免透题。讲解时同样原则：讲"为什么"和"要满足什么"，把"怎么写"留给用户。
  - 允许给：设计思路（为什么这么做）、关键不变量（必须满足的性质）、纠错点（易错处）、自查点（写完怎么验证）、必要时点一下关键 API 名。
  - 禁止给：步骤化指引（"第 1 步做 X"）、具体语句/表达式、变量名、返回值与错误处理的写法。给了这些就变成抄写作业。
  - 正反例（同一方法 Insert，链表）：
    - ❌ 细粒度（透题）：`// 第 1 步：node := &Node{Val: v}` + `// 第 2 步：node.Next = cur.Next; cur.Next = node` ...
    - ✅ 粗粒度（引导）：`// 思路：先让新节点接好后继，再让前驱指向新节点；接的顺序错了会丢链。` + `// 不变量：插入后从头遍历仍能完整走到尾，新节点恰好位于前驱与后继之间。` + `// 自查：在头、中、尾各插一次，遍历输出顺序符合预期。`
- 高杠杆优先：review 时聚焦影响正确性、复杂度与维护成本的问题（如边界处理缺失、越界、死循环、复杂度退化）；风格琐碎不影响结果的问题直接忽略。
- 用户明确表示忽略的问题不再重复提出。

### 版本迭代循环（单版本完整流程）

每版按以下循环推进，每步完成即 commit：

1. **AI 打地基**：结构体与方法签名预先立好，方法体留 `panic("TODO")`，注释给粗粒度提示（设计思路、不变量、纠错点），保证 build/vet 全绿。
2. **用户手搓实现**：用户按思路自行实现全部方法。
3. **AI review → 用户修订**：review 聚焦正确性与复杂度问题，可多轮迭代，直到逻辑无误。
4. **提交**：全绿后 commit（如 `feat: implement dynamic array`）。
5. **AI 优化示范版**：仅当用户明确要求时，AI 给出更优雅、更高性能的实现版本，并补全思路注释（为什么这么做、关键设计点、可对照学习的坑），供用户对照学习；随后 commit（如 `refactor: ...`）。无要求则跳过。
6. **AI 打测试地基**：给测试思路与覆盖点清单（边界、一致性、顺序、压力），具体测试用例由用户编写。
7. **用户写测试 → AI review**：review 测试是否覆盖关键场景与不变量，全绿后 commit。
8. **版本完成**：记录工时、更新顶层 `README.md` 进度，推进下一主题，回到第 1 步。

## 交付流程

在顶层 `README.md` 跟踪进度：一次只推进一个主题，更新状态标记，并在每个主题完成时记录实际工时；以任务清单的当前状态为准，不要凭假设。提交使用简洁的 Conventional Commit 风格（如 `feat: implement linked list`）。绝不提交临时文件或真实凭据。
